# Основы YAML: отступы, списки, словари

# Используются только пробелы, не табуляция
#Два пробела — стандарт для Ansible
  - name: Правильный отступ
    ping:


# Словари (ключ: значение)
name: nginx
state: present

# Списки (- перед элементом)
packages:
  - htop
  - git
  - curl
  
#Можно писать и так:
packages: [htop, git, curl]


# Скаляры: строки, числа, логика

# Строки
message: "Привет мир"

# Кавычки не обязательны, но нужны при:
#    пробелах
#    спецсимволах (:, @, ! и др.)

# Числа
port: 8080

# Логические значения
debug: true
maintenance: false

# Многострочные строки
# С сохранением форматирования (|)
content: |
  Первая строка
  Вторая строка

# С объединением в одну строку (>)
content: >
  Это будет одна
  строка в файле
